public class UserLogin {
    public void loginUser(string username) {
        string user = null;
        // No null check, guaranteed to fail!
        if(user.Length > 0) { 
            // do nothing
        }
    }
}
