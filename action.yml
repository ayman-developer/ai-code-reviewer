name: 'AI Code Reviewer'
description: 'Review PRs using AI (Gemini)'
author: 'AI Assistant'
inputs:
  github_token:
    description: 'GitHub token'
    required: true
  gemini_api_key:
    description: 'Gemini API key'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
